catchup -- what if some are already pending?

document sqlite3 stuff

settitle

OPML import

nicer display
  -- libcurl?

set id3 before rename

Need to handle curl exiting successfully without creating the file

don't have blank titles in downloaded files
