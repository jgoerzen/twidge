Move episode in-place atomically:

  First, move to temporary name, but in dest dir

  Then, set id3 tags

  Then rename to final name

castid lists -- filter out dupes post-process

catchup -- what if some are already pending?

